<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ytu.icoding.dao.BonusPenaltyDao">

	<insert id="insertPenalty" parameterType="java.util.HashMap">
		insert bonusPenalty(userId,type,price,createDate) values (#{userId},#{type},#{price},curdate())
	</insert>

</mapper>